<%= form_for @song do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= params.inspect %>
  <%= artist_select(@song, params) %>
  <br />

  <%= f.submit %>
<% end %>
